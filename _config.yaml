name: "Suite Knowledge!"
title: "Suite Knowledge!"

encoding: UTF-8
kramdown:
  syntax_highlighter_opts:
    disable: true

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag